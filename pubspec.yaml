name: qrcode_read_and_ganerate
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: 
  flutter_linkify: 
  image_gallery_saver: 
  permission_handler:
  qr_flutter: 
  qrscan: 0.3.3
  url_launcher:
  video_player: 2.4.6 

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
